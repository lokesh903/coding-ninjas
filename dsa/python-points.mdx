ord("A") = gives ascii value of A
char(123) = gives char at ascii value 123

a = "abcdef" >= "abcd" , is True because => 
The expression "abcdef" >= "abcd" returns True in Python because it is comparing two strings lexicographically (dictionary order)

st = "hello"
st[0] = 'r' it wont work 

